/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { Work } from "./Work";
import { WorkCountArgs } from "./WorkCountArgs";
import { WorkFindManyArgs } from "./WorkFindManyArgs";
import { WorkFindUniqueArgs } from "./WorkFindUniqueArgs";
import { CreateWorkArgs } from "./CreateWorkArgs";
import { UpdateWorkArgs } from "./UpdateWorkArgs";
import { DeleteWorkArgs } from "./DeleteWorkArgs";
import { CategoryOfWork } from "../../categoryOfWork/base/CategoryOfWork";
import { WorkService } from "../work.service";
@graphql.Resolver(() => Work)
export class WorkResolverBase {
  constructor(protected readonly service: WorkService) {}

  async _worksMeta(
    @graphql.Args() args: WorkCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [Work])
  async works(@graphql.Args() args: WorkFindManyArgs): Promise<Work[]> {
    return this.service.works(args);
  }

  @graphql.Query(() => Work, { nullable: true })
  async work(@graphql.Args() args: WorkFindUniqueArgs): Promise<Work | null> {
    const result = await this.service.work(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Work)
  async createWork(@graphql.Args() args: CreateWorkArgs): Promise<Work> {
    return await this.service.createWork({
      ...args,
      data: {
        ...args.data,

        categoryOfWork: args.data.categoryOfWork
          ? {
              connect: args.data.categoryOfWork,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => Work)
  async updateWork(@graphql.Args() args: UpdateWorkArgs): Promise<Work | null> {
    try {
      return await this.service.updateWork({
        ...args,
        data: {
          ...args.data,

          categoryOfWork: args.data.categoryOfWork
            ? {
                connect: args.data.categoryOfWork,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Work)
  async deleteWork(@graphql.Args() args: DeleteWorkArgs): Promise<Work | null> {
    try {
      return await this.service.deleteWork(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => CategoryOfWork, {
    nullable: true,
    name: "categoryOfWork",
  })
  async getCategoryOfWork(
    @graphql.Parent() parent: Work
  ): Promise<CategoryOfWork | null> {
    const result = await this.service.getCategoryOfWork(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
